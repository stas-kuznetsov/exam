pipeline {
agent any
    stages {

  
                    stage('Dependency Check') {
                     steps {
                    mvn 'org.owasp:dependency-check-maven:check -Ddependency-check-format=HTML'
                    step([$class: 'DependencyCheckPublisher', unstableTotalAll: '0'])
                }  
         }      
  }
}
